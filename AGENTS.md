# Codex private-workspace instructions

This is the private companion repository for the public project in `..`.
Read `../AGENTS.md`, `../CLAUDE.md`, and the task-relevant route from
`../docs/START_HERE.md` before editing data here.

- Follow the version-control completion policy in `CLAUDE.md`: automatically
  commit each completed step in every affected repository, and do not continue
  sequential work until those commits succeed.
- Never stage secrets, credentials, browser cookies, downloaded audio, or
  unrelated user changes.
